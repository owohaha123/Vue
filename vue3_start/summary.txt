▶ vue 설치방법
- Vue를 설치할 수 있는 방법은 크게 3가지

1. 페이지에 CDN package로 import 하기
  - 프로토타이핑 또는 학습 목적이라면, 아래 코드로 최신 버전을 사용 가능
	<script src="https://unpkg.com/vue@next"></script>
	<!-- 또는 -->
	<script src="https://unpkg.com/vue@3.2.31"></script>

2. npm을 사용하여 import 하기
  - Vue를 사용하여 대규모 애플리케이션을 구축할 때 NPM를 이용한 설치를 권장
  - NPM은 Webpack 또는 Browserify와 같은 모듈 번들러와 잘 작동함
	npm install vue@next

3. 공식 CLI를 사용
  - 이 방법으로 프로젝트를 scaffold하고
  - 최신 프론트엔드 워크플로우(예. hot-reload, lint-on-save 등)를 위한 batteries-included build를 제공
  - Vue CLI
     > Vue CLI는 웹팩 기반 빌드도구지만 Vue CLI는 현재 유지관리 모드에 있음
     > 특정 특정 웹팩 기능에 의존하지 않는 한 Vite로 새 프로젝트를 시작하는 것이 좋습니다.
     > Vue CLI를 사용하기 위해서는 `@vue/cli` v4.5 이상의 버전을 설치해야 함
	npm install -g @vue/cli

---------------------------------------------------
+ Vite
- cdn 이 아닌 npm 방식으로 관리
	npm init -y
	npm i vue 
	npm i vite 

- 플러그인
	npm i @vitejs/plugin-vue << 안 해도 될 지도

- vite.config.js 파일 생성

- package.json 
	"scripts": {
		"dev" : "vite", -- 추가
		"test": "echo \"Error: no test specified\" && exit 1"
	},

- npm run vite dev

--> 서버 잘 뜨면 good
     import 했던 vue 스크립트(CDN)는 주석해도 됨